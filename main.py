import logging
from funcs import *
from utils import *
from pathlib import Path
logging.basicConfig(level=logging.NOTSET)

if __name__ == "__main__":
	run_mod("Exit Music Redux 1.1", skip_splash_screen=True)